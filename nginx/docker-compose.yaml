version: "3.8"

services:
  nginx:
    image: nginx:latest
    container_name: webserver
    restart: unless-stopped
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./nginx/mounts/etc/nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/mounts/opt/ssl:/opt/ssl/
